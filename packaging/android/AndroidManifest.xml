<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mixxx"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
    <uses-permission android:name="android.permission.INTERNET"  />
    <!-- API 33+ (Android 13) replaced the broad READ/WRITE_EXTERNAL_STORAGE
         model with per-mediatype permissions; READ_MEDIA_AUDIO is what we
         actually need to scan a music library. The legacy permissions are
         kept with maxSdkVersion guards so devices on Android 12 and below
         (and side-loaded builds on AOSP forks without scoped storage)
         still work. MANAGE_EXTERNAL_STORAGE remains unguarded because
         Mixxx genuinely needs filesystem-wide access to recursively scan
         user-chosen folders; Google Play scrutinizes this permission, so
         non-Play distributions are the primary target. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"  />
    <uses-permission android:name="android.permission.RECORD_AUDIO"  />
    <uses-permission android:name="android.permission.USB_PERMISSION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Bluetooth/Low Energy MIDI controller support (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- WiFi-based controller support (Android 13+) -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <!-- BLE feature is required for Bluetooth MIDI controllers -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true"
        android:resizeable="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DJ Sugar"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:fullBackupOnly="false"
        android:extractNativeLibs="false">
        <activity
            android:name="org.mixxx.MainActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="true"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

          <meta-data
                android:name="android.app.lib_name"
                android:value="mixxx" />

          <meta-data
                android:name="android.app.background_running"
                android:value="true" />

          <meta-data
                android:name="android.app.splash_screen_drawable"
                android:value="@drawable/ic_launcher_foreground" />

          <!-- Run the standard widget UI (MixxxApplication + LateNight skin)
               on Android. Do NOT add the new_ui or qml command line flags
               here: the QML UI is experimental and renders a broken,
               overlapping layout on phones (controls on top of each other,
               empty library, UI lost on rotation). The legacy skin engine
               works on Android with Qt 6 and is the supported path for this
               fork. Keep the log level in sync with
               QT_ANDROID_APPLICATION_ARGUMENTS in CMakeLists.txt.
               Note: XML comments cannot contain a double dash, so the
               flag names above are written without their leading dashes. -->
          <meta-data
                android:name="android.app.arguments"
                android:value="--log-level warning" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths"/>
        </provider>
    </application>

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="true" />
    <!-- Declare that we benefit from low-latency audio hardware. This is not
         required (app still works without it) but lets the system/Play Store
         know we're optimized for pro-audio devices. -->
    <uses-feature
        android:name="android.hardware.audio.low_latency"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.audio.pro"
        android:required="false" />
</manifest>
