To install from source on linux you need the libraries mentioned in the README file.

Compile and install using the following commands:
cd src
qmake mixxx.pro
make
cp -r config/* /usr/share/mixxx
cp mixxx /usr/bin
