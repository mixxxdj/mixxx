syntax = "proto2";

package mixx.track.io;

option optimize_for = LITE_RUNTIME;

message Macro {
  message Action {
    optional uint64 origin = 1;
    required bytes data = 2;
  }
  repeated Action actions = 1;
}
